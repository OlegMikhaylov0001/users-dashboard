import { SettingsApp } from "../components/settings/SettingsApp";

export const metadata = {
  title: "Workspace Settings — UserBase",
};

export default function SettingsPage() {
  return <SettingsApp />;
}
