{
  "name": "users-dashboard-chat-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that proxies the dashboard chat to OpenRouter free-tier models.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "secret:put": "wrangler secret put OPENROUTER_KEY"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^5",
    "wrangler": "^4"
  }
}
